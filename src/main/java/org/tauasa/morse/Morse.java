package org.tauasa.morse;

public class Morse {

    protected static final String[] MORSE_CODE = {
        ".-", "-...", "-.-.", "-..", ".", "..-.", "--.", "....", "..", ".---",
        "-.-", ".-..", "--", "-.", "---", ".--.", "--.-", ".-.", "...", "-",
        "..-", "...-", ".--", "-..-", "-.--", "--.."
    };

    public String charToCode(char c){
        return null;
    }

    public String codeToChar(String code){
        return null;
    }

    public String encode(String text){

        //split the text

        //iterate through the tokens and encode each character

        //return the encoded string
        return null;

    }

    public String decode(String text){

        //split the text

        //iterate through the tokens and encode each character

        //return the encoded string
        return null;

    }

}
